// js/config.js

/**
 * Supabase Project Configuration
 * 
 * Fill in these credentials to connect the sketchbook to your live database.
 * If these remain empty, the application will automatically run in local
 * mock-data mode (Phase 1 / Offline mode) so you can test the frontend.
 */
export const SUPABASE_URL = "";
export const SUPABASE_ANON_KEY = "";
